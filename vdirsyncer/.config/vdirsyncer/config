# Configuration for vdirsyncer.
#
# Move it to ~/.vdirsyncer/config or ~/.config/vdirsyncer/config and edit it.
# Run `vdirsyncer --help` for CLI usage.
#
# Optional parameters are commented out.
# This file doesn't document all available parameters, see
# http://vdirsyncer.readthedocs.io/ for the rest of them.

[general]
# A folder where vdirsyncer can store some metadata about each pair.
status_path = "~/.local/share/vdirsyncer/status/"
# Force Python 2 (must be removed in later versions)
#python2 = true

# === CARDDAV ===
[pair posteo_contacts]
# A `[pair <name>]` block defines two storages `a` and `b` that should be
# synchronized. The definition of these storages follows in `[storage <name>]`
# blocks. This is similar to accounts in OfflineIMAP.
a = "posteo_contacts_local"
b = "posteo_contacts_remote"

# Synchronize all collections that can be found.
# You need to run `vdirsyncer discover` if new calendars/addressbooks are added
# on the server.
collections = ["from a", "from b"]

# Synchronize the "display name" property into a local file (~/.local/share/contacts/displayname).
#metadata = ["displayname"]

# To resolve a conflict the following values are possible:
#   `null` - abort when collisions occur (default)
#   `"a wins"` - assume a's items to be more up-to-date
#   `"b wins"` - assume b's items to be more up-to-date
conflict_resolution = "b wins"

[storage posteo_contacts_local]
# A storage references actual data on a remote server or on the local disk.
# Similar to repositories in OfflineIMAP.
type = "filesystem"
path = "~/.local/share/contacts/"
fileext = ".vcf"

[storage posteo_contacts_remote]
type = "carddav"
url = "https://posteo.de:8843/addressbooks/hagen.wierstorf/default"
username = "hagen.wierstorf@posteo.de"
# The password can also be fetched from the system password storage, netrc or a
# custom command. See http://vdirsyncer.readthedocs.io/en/stable/keyring.html
#password = ""
password.fetch = ["command", "pass", "mail/posteo.de"]

# === CALDAV ===
# Calendars also have a color property
#metadata = ["displayname", "color"]

# === Posteo
[pair posteo_calendar]
a = "posteo_calendar_local"
b = "posteo_calendar_remote"
collections = ["from a", "from b"]

[storage posteo_calendar_local]
type = "filesystem"
path = "~/.local/share/calendars/"
fileext = ".ics"

[storage posteo_calendar_remote]
type = "caldav"
url = "https://posteo.de:8443/calendars/hagen.wierstorf/default"
username = "hagen.wierstorf@posteo.de"
#password = ""
password.fetch = ["command", "pass", "mail/posteo.de"]

# == audEERING
#[pair audeering_calendar]
#a = "audeering_calendar_local"
#b = "audeering_calendar_remote"
#collections = ["from a", "from b"]
#
#[storage audeering_calendar_local]
#type = "filesystem"
#path = "~/.local/share/calendars/"
#fileext = ".ics"
#
#[storage audeering_calendar_remote]
#type = "caldav"
#url = "https://dav.ox.hosteurope.de/caldav/225"
#username = "hagen.wierstorf@posteo.de"
##password = ""
#password.fetch = ["command", "pass", "mail/posteo.de"]
